{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd spring-backend && ./gradlew clean bootJar -x test"
  },
  "deploy": {
    "startCommand": "java -jar spring-backend/build/libs/*.jar"
  }
}
